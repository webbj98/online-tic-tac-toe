// A place for emit functions that I haven't figured out where else to put them

import { Socket } from "socket.io";
import { Message } from "../../../shared/src/model.js";

export function sendMessage(socket: Socket, message: Message) {


}